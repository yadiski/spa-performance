import { z } from 'zod';

// Stub schemas — T6–T10 will fill these in with real structure.

export const staffSummarySchema = z.object({}).passthrough();

export const kraQualitySchema = z.object({}).passthrough();

export const devRecommendationsSchema = z.object({}).passthrough();

export const calibrationSchema = z.object({}).passthrough();

export const midYearNudgesSchema = z.object({}).passthrough();
